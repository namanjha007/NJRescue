<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="NJ Covid Rescue"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".donate_activities.DonateCash"></activity>
        <activity android:name=".donate_activities.DonatePlasma" />
        <activity android:name=".DonateActivity" />
        <activity
            android:name=".resource_activities.DonateActivity"
            android:label="@string/title_activity_donate"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity android:name=".VaccineActivity" />
        <activity android:name=".resource_activities.MiscellaneousActivity" />
        <activity android:name=".resource_activities.MedicineActivity" />
        <activity android:name=".resource_activities.BedsActivity" />
        <activity android:name=".resource_activities.AmbulanceActivity" />
        <activity android:name=".resource_activities.OxygenActivity" />
        <activity android:name=".ResourcesActivity" />
        <activity
            android:name=".StartOptionActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>